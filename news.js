// Import the q module
let q = require('q');
// Import the node-readability module
let nr = require('node-readability');
// Import the RSS/XML feed-read module
let fr = require('feed-read');

let app = (function() {
  'use strict';  // function level strict mode
  
  let _func = function() {
    let d = q.defer();
    try {
      
    } catch(ex) { d.reject({msg: `() exception : ${ex}`, ex: ex}); }
    return d.promise;
  };
  
  let _func = function() {
    let d = q.defer();
    try {
      
    } catch(ex) { d.reject({msg: `() exception : ${ex}`, ex: ex}); }
    return d.promise;
  };
  
  let _func = function() {
    let d = q.defer();
    try {
      
    } catch(ex) { d.reject({msg: `() exception : ${ex}`, ex: ex}); }
    return d.promise;
  };
  
  let _func = function() {
    let d = q.defer();
    try {
      
    } catch(ex) { d.reject({msg: `() exception : ${ex}`, ex: ex}); }
    return d.promise;
  };
  
  // Entry point into the application
  let _main = function() {
      
  };
  
  // return entry point into the application
  return { main: _main};
})();

// Start the App
app.main();